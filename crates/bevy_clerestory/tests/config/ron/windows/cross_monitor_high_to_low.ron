(
    version: 2,
    entries: [
        (
            key: Primary,
            state: (
                // Saved on Monitor 1 (low-DPI), launched from Monitor 0 (high-DPI)
                // Tests CompensateSizeOnly strategy
                logical_position: Some((${MONITOR_1_LOGICAL_POS_X}, ${MONITOR_1_LOGICAL_POS_Y})),
                logical_width: 1200,
                logical_height: 900,
                monitor_scale: 1.0,
                monitor_index: 1,
                mode: Windowed,
                app_name: "restore_window",
            )
        ),
    ],
)
