//! Failure domain: missing-file evidence, same-frame failure ordering, mixed
//! outcomes, empty-set panics, and the exit-on-failure pattern.

mod declaration_order;
mod empty_set;
mod missing_file;
mod mixed_outcome;
mod same_frame_pair;
